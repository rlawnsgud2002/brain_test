# Development / test-only dependencies
# Runtime dependencies are documented in the README and installed on demand.
#
# Install:   pip install -r requirements-dev.txt
# Run tests: pytest
pytest>=8.0
pytest-asyncio>=0.23
numpy>=1.24
scipy>=1.10
